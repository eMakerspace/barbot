{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build rust esp32",
            "type": "cargo",
            "command": "build",
            "options": {
                "cwd": "esp32/barbotv2"
            },
            "problemMatcher": "$rustc",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}